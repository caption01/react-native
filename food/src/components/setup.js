import React from "react";
import { View, Text, StyleSheet } from "react-native";

const Naming = () => {
  return (
    <View>
      <Text>Naming Screen !</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Naming;
